const app=document.querySelector('#app'),nav=document.querySelector('#nav'),account=document.querySelector('#account'),modal=document.querySelector('#modal');let me=null;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function request(url,options={}){const r=await fetch(url,{headers:{'content-type':'application/json',...(options.headers||{})},...options});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`Request failed (${r.status})`);return data}
const avatar=u=>u.headshot||`https://www.roblox.com/headshot-thumbnail/image?userId=${u.roblox_user_id}&width=150&height=150&format=png`;
function chrome(){nav.innerHTML=me?.authenticated?'<a href="/dashboard">Home</a><a href="/applications">Applications</a>':'<a href="/">Login</a><a href="/applications">Applications</a>';for(const a of nav.children)if(a.pathname===location.pathname)a.classList.add('active');account.innerHTML=me?.authenticated?`<div class="account"><img class="avatar" src="${avatar(me)}" alt=""><button class="logout" id="logout" type="button">Log Out</button></div>`:'';const logout=document.querySelector('#logout');if(logout)logout.onclick=()=>showModal('Log Out','Are you sure you would like to log out?',async()=>location.assign('/auth/logout'))}
function showModal(title,text,confirm){modal.classList.remove('hidden');modal.innerHTML=`<div class="dialog"><h2>${esc(title)}</h2><p class="muted">${esc(text)}</p><div class="actions"><button class="ghost" id="cancel">Cancel</button><button class="primary" id="confirm">Confirm</button></div></div>`;modal.querySelector('#cancel').onclick=()=>modal.classList.add('hidden');modal.querySelector('#confirm').onclick=async()=>{modal.classList.add('hidden');await confirm()}}
async function login(){const error=new URLSearchParams(location.search).get('error');app.innerHTML=`<div class="login-shell"><section class="login-card"><img class="mark" src="/images/InitialTypography.png"><div class="portal-title">CONJURES PORTAL</div><p class="muted">Log in with a verified account to access applications and your dashboard.</p>${error?`<p class="error">${error==='verification_required'?"You're not verified on Discord. Please ensure you verify and link your Roblox account in the CONJURES Discord server, then you may proceed to log in here.":'Login could not be completed. Please try again.'}</p>`:''}<div class="auth-stack"><a class="auth discord" href="/auth/discord"><img src="/images/Discord.png" alt="">Log in with Discord</a><a class="auth" href="/auth/roblox"><img src="/images/Roblox.png" alt="">Log in with Roblox</a></div></section></div>`}
async function dashboard(){if(!me.authenticated)return login();app.innerHTML=`<section class="hero"><div class="eyebrow">Dashboard</div><h1>Welcome to <span class="conjures-glow">CONJURES</span></h1><p class="muted">Your central place for CONJURES applications and account access.</p></section><section class="profile-card"><img class="avatar" src="${avatar(me)}"><div><h1>${esc(me.roblox_username)}</h1><p>${esc(me.rank.name)}</p><p class="muted">Roblox ID: ${esc(me.roblox_user_id)} · Discord ID: ${esc(me.discord_id)}</p></div></section>`}
async function applications(){const forms=await request('/api/applications');app.innerHTML=`<div class="section-head"><div><div class="eyebrow">Open opportunities</div><h1>Applications</h1><p class="muted">Explore currently available CONJURES team applications.</p></div></div>${forms.length?`<div class="application-grid">${forms.map(f=>`<article class="application-card" style="--team:${esc(f.team_color)}"><span class="tag">${esc(f.team_label)}</span><h3>${esc(f.name)}</h3><p class="muted">${f.schema.reduce((n,s)=>n+(s.questions?.filter(q=>!q.locked).length||0),0)} Questions</p><div class="card-meta"><b>CONJURES</b><div class="actions">${f.canEdit?`<a class="ghost" href="/applications/${f.id}/edit">Edit</a>`:''}${!me.authenticated?`<a class="primary" href="/">Log in to Apply</a>`:f.canApply?`<a class="primary" href="/applications/${f.id}">Apply Now</a>`:'<span class="disabled">You are unable to apply</span>'}</div></div></article>`).join('')}</div>`:`<div class="empty-state"><div class="empty-icon">!</div><p>All applications are currently closed. We encourage you to stay engaged in our communications server to be notified when our applications are released.</p></div>`}`}
function inputFor(q,value=''){if(q.locked)return `<input value="${esc(value)}" disabled>`;if(q.type==='long')return `<textarea data-q="${q.id}" ${q.required?'required':''}>${esc(value)}</textarea>`;if(q.type==='multiple'||q.type==='checkboxes')return `<div class="choices">${(q.options||[]).map(o=>`<label class="choice"><input data-q="${q.id}" type="${q.type==='multiple'?'radio':'checkbox'}" name="${q.id}" value="${esc(o)}"> ${esc(o)}</label>`).join('')}</div>`;if(q.type==='dropdown')return `<select data-q="${q.id}"><option value="">Select an option</option>${(q.options||[]).map(o=>`<option>${esc(o)}</option>`).join('')}</select>`;return `<input data-q="${q.id}" value="${esc(value)}" ${q.required?'required':''}>`}
async function applyPage(formId){const f=await request(`/api/applications/${formId}`);if(!me.authenticated||!f.canApply){app.innerHTML='<div class="success"><h1>You are unable to apply</h1><p class="muted">This application is unavailable for your account.</p></div>';return}app.innerHTML=`<div class="form-shell"><a class="back" href="/applications">← Back to Applications</a><div class="form-title"><span class="tag" style="--team:${esc(f.team_color)}">${esc(f.team_label)}</span><h1>${esc(f.name)}</h1><p class="description">${esc(f.description)}</p></div><form id="application-form">${f.schema.map(s=>`<section class="section"><h2>${esc(s.title)}</h2><p class="muted">${esc(s.description||'')}</p>${(s.questions||[]).map(q=>`<div class="question"><label>${esc(q.title)} ${q.required?'<span class="required">*</span>':''}</label>${q.description?`<small>${esc(q.description)}</small>`:''}${inputFor(q,q.id==='roblox_username'?me.roblox_username:q.id==='roblox_id'?me.roblox_user_id:q.id==='discord_id'?me.discord_id:'')}</div>`).join('')}</section>`).join('')}<div class="submit-row"><button type="submit" class="primary">Submit Application</button></div></form></div>`;document.querySelector('#application-form').onsubmit=e=>{e.preventDefault();showModal('Submit Application','Are you sure you would like to submit this application? You cannot edit it after submission.',async()=>{const answers={};document.querySelectorAll('[data-q]').forEach(el=>{if(el.type==='checkbox'){if(el.checked)(answers[el.dataset.q]??=[]).push(el.value)}else if(el.type!=='radio'||el.checked)answers[el.dataset.q]=el.value});try{await request(`/api/applications/${formId}/submit`,{method:'POST',body:JSON.stringify({answers})});app.innerHTML='<div class="success"><div class="check">✓</div><h1>Application submitted</h1><p>Your application has been successfully submitted. Please ensure you follow the guidelines of the application via the announcement. Best of luck!</p></div>'}catch(err){alert(err.message)}})}}
const uid=()=>crypto.randomUUID();
function editorQuestion(q,si,qi){return `<div class="question-editor" draggable="${!q.locked}" data-si="${si}" data-qi="${qi}"><div class="row"><input class="q-title" value="${esc(q.title)}" ${q.locked?'disabled':''}><select class="q-type" ${q.locked?'disabled':''}>${['short','long','multiple','checkboxes','dropdown'].map(t=>`<option value="${t}" ${q.type===t?'selected':''}>${({short:'Short answer',long:'Long answer',multiple:'Multiple choice',checkboxes:'Checkboxes',dropdown:'Dropdown'})[t]}</option>`).join('')}</select></div><input class="q-desc" placeholder="Optional question description" value="${esc(q.description||'')}" ${q.locked?'disabled':''}>${['multiple','checkboxes','dropdown'].includes(q.type)?`<textarea class="q-options" placeholder="One option per line">${esc((q.options||[]).join('\n'))}</textarea>`:''}<div class="editor-controls"><span class="drag">⋮⋮ Drag</span><label class="switch"><input class="q-required" type="checkbox" ${q.required?'checked':''} ${q.locked?'disabled':''}> Required</label>${q.locked?'':`<button type="button" class="ghost tiny duplicate">Duplicate</button><button type="button" class="ghost tiny delete-q">Delete</button>`}</div></div>`}
async function editPage(formId){const f=await request(`/api/applications/${formId}`);if(!f.canEdit)throw new Error('You cannot edit this application.');let schema=structuredClone(f.schema),description=f.description,isOpen=f.is_open,dirty=false,drag=null;const render=()=>{app.innerHTML=`<div class="form-shell"><div class="editor-toolbar"><a class="back" href="/applications">← Applications</a><div class="actions"><button class="ghost" id="discard">Discard Changes</button><button class="primary" id="save">Save Changes</button></div></div><div class="form-title"><div class="eyebrow">Application editor</div><h1>${esc(f.name)}</h1><label>Description</label><textarea id="form-description">${esc(description)}</textarea><label class="switch"><input id="is-open" type="checkbox" ${isOpen?'checked':''}> Open to public applications</label></div><div id="sections">${schema.map((s,si)=>`<section class="section editor-section" data-si="${si}" draggable="${!s.locked}"><input class="section-title" value="${esc(s.title)}" ${s.locked?'disabled':''}><textarea class="section-desc" placeholder="Optional section description" ${s.locked?'disabled':''}>${esc(s.description||'')}</textarea>${(s.questions||[]).map((q,qi)=>editorQuestion(q,si,qi)).join('')}<div class="editor-controls">${s.locked?'':`<button class="ghost tiny delete-section" type="button">Delete Section</button>`}<button class="ghost tiny add-question" type="button">+ Add Question</button></div></section>`).join('')}</div><button class="ghost" id="add-section">+ Add Section</button></div>`;bind()};
function sync(){description=document.querySelector('#form-description').value;isOpen=document.querySelector('#is-open').checked;document.querySelectorAll('.editor-section').forEach((el,si)=>{const s=schema[si];s.title=el.querySelector('.section-title').value;s.description=el.querySelector('.section-desc').value;el.querySelectorAll('.question-editor').forEach((qe,qi)=>{const q=s.questions[qi];q.title=qe.querySelector('.q-title').value;q.type=qe.querySelector('.q-type').value;q.description=qe.querySelector('.q-desc').value;q.required=qe.querySelector('.q-required').checked;const opts=qe.querySelector('.q-options');q.options=opts?opts.value.split('\n').map(x=>x.trim()).filter(Boolean):[]})});dirty=true}
function bind(){document.querySelectorAll('input,textarea,select').forEach(x=>x.oninput=sync);document.querySelectorAll('.add-question').forEach((b,si)=>b.onclick=()=>{sync();schema[si].questions.push({id:uid(),title:'Untitled Question',type:'short',required:false,description:''});render()});document.querySelectorAll('.delete-section').forEach(b=>b.onclick=()=>{sync();schema.splice(Number(b.closest('.section').dataset.si),1);render()});document.querySelectorAll('.delete-q').forEach(b=>b.onclick=()=>{sync();const e=b.closest('.question-editor');schema[+e.dataset.si].questions.splice(+e.dataset.qi,1);render()});document.querySelectorAll('.duplicate').forEach(b=>b.onclick=()=>{sync();const e=b.closest('.question-editor'),q=structuredClone(schema[+e.dataset.si].questions[+e.dataset.qi]);q.id=uid();schema[+e.dataset.si].questions.splice(+e.dataset.qi+1,0,q);render()});document.querySelectorAll('.q-type').forEach(x=>x.onchange=()=>{sync();render()});document.querySelectorAll('.question-editor').forEach(el=>{el.ondragstart=e=>{drag={si:+el.dataset.si,qi:+el.dataset.qi};e.stopPropagation()};el.ondragover=e=>e.preventDefault();el.ondrop=e=>{e.preventDefault();e.stopPropagation();if(!drag)return;sync();const target={si:+el.dataset.si,qi:+el.dataset.qi},[item]=schema[drag.si].questions.splice(drag.qi,1);schema[target.si].questions.splice(target.qi,0,item);drag=null;render()}});document.querySelectorAll('.editor-section').forEach(el=>{if(el.dataset.si==='0')return;el.ondragstart=e=>{if(e.target!==el)return;drag={section:+el.dataset.si}};el.ondragover=e=>e.preventDefault();el.ondrop=e=>{if(drag?.section==null)return;e.preventDefault();sync();const to=+el.dataset.si,[item]=schema.splice(drag.section,1);schema.splice(to,0,item);drag=null;render()}});document.querySelector('#add-section').onclick=()=>{sync();schema.push({id:uid(),title:'Untitled Section',description:'',questions:[]});render()};document.querySelector('#discard').onclick=()=>location.reload();document.querySelector('#save').onclick=async()=>{sync();await request(`/api/applications/${formId}`,{method:'PUT',body:JSON.stringify({description,isOpen,schema})});dirty=false;alert('Changes saved successfully.')};window.onbeforeunload=()=>dirty?'You have unsaved changes.':undefined}render()}
function legal(type){const privacy=type==='privacy';app.innerHTML=`<article class="legal"><div class="eyebrow">CONJURES</div><h1>${privacy?'Privacy Policy':'Terms of Service'}</h1><p>Last updated August 27, 2026.</p>${privacy?`<h2>Information we collect</h2><p>We process your linked Discord and Roblox identifiers, usernames, group rank, server roles, application answers, and application outcomes to operate CONJURES applications.</p><h2>How information is used</h2><p>Information is used to authenticate you, determine eligibility, review applications, prevent duplicate submissions, and protect the service. Application responses are shared only with authorized CONJURES reviewers.</p><h2>Retention</h2><p>Pending applications are retained while under review. Denial metadata may be retained for seven days to enforce the reapplication period. Operational and security records may be retained when reasonably necessary.</p><h2>Your choices</h2><p>You may log out at any time. Contact CONJURES through a support ticket for questions or appropriate data requests.</p>`:`<h2>Eligibility and accounts</h2><p>You must use a Discord and Roblox account linked through the official CONJURES verification system. You are responsible for the accuracy of information submitted through your account.</p><h2>Applications</h2><p>Applications must be truthful, original, and comply with all CONJURES rules. Submission does not guarantee acceptance. Reviewer decisions may be made according to current staffing needs and eligibility requirements.</p><h2>Acceptable use</h2><p>You may not bypass restrictions, interfere with the website, impersonate another person, automate submissions, or attempt to access restricted editing and review functions.</p><h2>Availability</h2><p>CONJURES may modify, suspend, or discontinue applications and site features when necessary.</p>`}</article>`}
async function route(){me=await request('/api/me');chrome();const p=location.pathname;try{if(p==='/')return me.authenticated?dashboard():login();if(p==='/dashboard')return dashboard();if(p==='/applications')return applications();if(p==='/terms'||p==='/privacy')return legal(p.slice(1));const m=p.match(/^\/applications\/([^/]+)(\/edit)?$/);if(m)return m[2]?editPage(m[1]):applyPage(m[1]);return applications()}catch(e){app.innerHTML=`<div class="success"><h1>Something went wrong</h1><p class="muted">${esc(e.message)}</p></div>`}}route();
